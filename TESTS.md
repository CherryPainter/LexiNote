# 验证与手动测试说明（TESTS.md）

本文档提供手动测试步骤，用于验证设置在运行时即时生效的行为。适合 QA 或开发在本地快速核查。

## 前置条件

- 在 Windows 下使用 PowerShell（项目测试环境）。
- 已安装依赖：`pip install -r requirements.txt`。
- 可选：启动 Ollama 服务以验证 AI 相关行为。

## 测试 1：翻译练习模块运行时切换 Auto/Manual

1. 启动程序： `python main.py`。
2. 打开“翻译练习”页面并开始练习一个或两个单词。
3. 打开“设置”页面，将 `翻译练习` 模式切换为“自动”。
4. 观察翻译页面：
   - 如果当前单词刚显示结果且 `auto_next_correct/auto_next_wrong` 为 True，应在 `auto_next_delay` 毫秒后自动跳到下一题，并隐藏“下一步”按钮。
   - 切换回“手动”时，应立即显示“下一步”按钮。

## 测试 2：复习模块例句后的自动切换

1. 进入“单词复习”页面，确保当前单词有例句（或触发生成）。
2. 在设置中将 `复习` 模式切为“自动”，并将 `auto_next_example` 打开。
3. 在复习页面点击“显示例句”，应在 `auto_next_delay` 后自动跳转到下一个单词。

## 测试 3：听写模块（单词学习）模式切换

1. 进入“听写练习”页面并开始单个听写。
2. 在设置中将 `单词学习` 模式切换为“自动”。
3. 在当前题目完成（答对/答错）后，检查是否根据 `auto_next_correct/auto_next_wrong` 的设置自动跳转。

## 边界/异常场景

- 切换设置时页面尚未注册监听器（页面刚打开但初始化未完成）: 确认页面在初始化完成后读取当前设置并同步 UI 状态。
- 网络或 AI 不可用时: 翻译页面应回退到本地判断逻辑，同时设置变更仍应控制 UI（按钮显示/隐藏）。

## 自动化测试建议

- 可以在项目中添加 pytest 用例模拟 SettingsManager.set_setting，然后断言已注册回调被调用（使用 monkeypatch 或 mock 对象）。
- 对 tkinter 的 UI 行为建议使用集成测试或手动回归测试，因为 tkinter 在无头环境中测试较为困难。
